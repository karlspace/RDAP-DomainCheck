dist/
coverage/
node_modules/
package-lock.json

# Generated by semantic-release, which rewrites it on every release in its own
# format. Leaving it under prettier --check breaks the very next run after each
# release, which is exactly what happened for 1.0.0.
CHANGELOG.md
